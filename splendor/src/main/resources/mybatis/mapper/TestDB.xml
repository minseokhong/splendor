<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rng.splendor.db.mapper.TestMapper">
    <select id="getAll" resultType="com.rng.splendor.db.dto.Test">
    </select>
    
    <select id="insertTest" parameterType="com.rng.splendor.db.dto.Test">
    insert into test values (
    '${id}', 
    '${pw}'
    )
    </select>
</mapper>
